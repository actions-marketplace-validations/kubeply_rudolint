[alias]
xtask-check = "check --all-targets --all-features --locked"
xtask-clippy = "clippy --all-targets --all-features --locked -- -D warnings"
xtask-test = "test --all-targets --all-features --locked"
